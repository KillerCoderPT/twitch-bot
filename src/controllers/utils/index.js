module.exports.getHowManyHoursIsLive = (oldDate) => {
    const getStartedTime = ((oldDate.split("T"))[1]).slice(0, -1)
    // const currentHour = 
    console.log(getHour);
    // console.log(`Hour: ${hour} Minute: ${minute}`);
};
